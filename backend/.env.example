# Optional: use a MongoDB database. If omitted or unavailable, the app stores
# accounts and movies in backend/data/store.json instead.
MONGO_URI=
JWT_SECRET=replace-with-a-long-random-secret

# Port 5000 is occupied by macOS Control Center on some machines.
PORT=5001
